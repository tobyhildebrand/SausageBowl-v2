# Composer
/vendor/
composer.lock

# App config and secrets (both contain sensitive values, never commit)
/config/config.php
/config/secrets.php

# Yahoo OAuth token cache (tokens live in the DB; this file should not exist)
yahoo_oauth.json

# OS / editor artifacts
.DS_Store
Thumbs.db
.idea/
.vscode/

# PHP error logs and deployment logs
*.log

